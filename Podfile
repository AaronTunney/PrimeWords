# Uncomment the next line to define a global platform for your project
platform :ios, '13.0'

target 'PrimeWords' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for PrimeWords
  pod 'SwiftLint'
  pod 'RealmSwift'

  target 'PrimeWordsTests' do
    inherit! :search_paths
    # Pods for testing
  end
end
